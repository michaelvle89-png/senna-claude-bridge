## Claude thread — Dashboard Ownership (2026-09-24 ~15:55 PDT)

Thread: https://claude.ai/code/session_016xv7gyzJztLsYZxs3LTpFU (redirects to https://claude.ai/cowork/cse_016xv7gyzJztLsYZxs3LTpFU)
Newest assistant message (Message 5 of 5, heading "Claude responded: Found it."). Full text verbatim:

---

Found it. Look at this line from sniper_signals.jsonl — the actual signal that triggered this trade:

LEGACY LONG S/D MNQ1! off POC @ 30760.00 · x3 · SL 30743.75 (16.3 pt) · ind (TP) 30783.75 (1.5R)

The signal's own math is correct — SL 30743.75 sits 16.3 points below its reference entry of 30760.00, exactly as it should for a long. But look at the actual Tradovate fill: 30,720.75 — not 30,760.00. The market had drifted ~39 points lower while the desk was down.

Here's the mechanism: sniper_belt.py's _place() sends the market entry order, then submits the SL and TP as fixed absolute prices lifted straight from the original alert text — self.ex.place(..., stop=sl, target=target) — with no recheck against where the order actually filled. It never recomputes the stop relative to the real fill.

So: entry (market) filled at whatever price the market was at right now (30,720.75). The stop, still anchored to the stale signal's 30,760 reference point, landed at 30,743.75 — which is now above the real fill instead of below it. A sell-stop above the current price is invalid for a long (it would trigger instantly), so Tradovate rejected it outright. TP (30,783.75) still happened to be above the real fill too, so it was accepted as a working order until you flattened.

Root cause: the boot replayed a ~4-hour-old backlog of queued signals with stale absolute price levels, and nothing checks signal staleness or recomputes SL/TP relative to actual fill price before sending the bracket.

This is a structural gap, not a one-off fluke — any signal sitting in the queue while the desk is down is exposed to this the moment the desk comes back up armed/live.

I haven't touched any code. Two things worth deciding before I propose a fix:
1. Should stale signals (queued while the desk was down past some age threshold) be discarded on boot rather than replayed at all?
2. Should SL/TP be recomputed as an offset from the actual fill price (preserving the original point-distance) rather than sent as the stale absolute levels?

Want me to draft the fix for your go-ahead, or do you want to set the staleness rule yourself first?
