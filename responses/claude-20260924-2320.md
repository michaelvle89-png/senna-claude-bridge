# Claude cowork thread (Dashboard Ownership) — captured 2026-09-24 23:23 UTC (16:23 PDT)

Thread: https://claude.ai/code/session_016xv7gyzJztLsYZxs3LTpFU (redirects to https://claude.ai/cowork/cse_016xv7gyzJztLsYZxs3LTpFU)
Newest assistant message (Message 4 of 4, "Used 7 tools"). Full text verbatim:

---
Found the real constraint, and it changes what's buildable: `run_desk.py`'s live path is `TradersPostExecutor` — fire-and-forget webhook to TradersPost, no read-back from Tradovate at all. The belt never learns whether a bracket leg was accepted or rejected; that's why the naked position stayed invisible on the desk side and only showed up in your Tradovate UI. There's a separate `tradovate.py` native-API client, but it's Phase-1/demo-only and explicitly blocked on prop/eval accounts — not wired into live execution. So true after-the-fact confirmation isn't available right now without new broker integration work.

Given that, here's the two-part fix that actually implements what you said, using what the code already has:

**1. Missed signal → don't trade it.** Every signal already carries `_rx` (the real webhook receive timestamp). I'll gate entries on: if a signal's `_rx` predates this belt process's own boot time — meaning it arrived while the desk was down — refuse it outright, log it, send no order. Same refusal pattern as the existing unknown-contract/stop-too-tight/qty-cap checks. This is exactly what killed you tonight: a signal that sat for ~3h39m during the outage got replayed on boot as if it were live.

**2. Never send a bracket that's already wrong on its face.** Before placing ANY entry, check the proposed SL against the current live price (`last_close`, already passed into `on_signal`): for a LONG the SL must sit below it, for a SHORT it must sit above. If that fails — or if there's no live price to check against yet — refuse the entry, no order sent. This is a hard directional check, not a guessed threshold, so it catches this bug regardless of cause (staleness or anything else) and stops the desk from ever submitting an inverted stop again.

Neither of these requires touching Fearless files or execution_mode/flatten. Both live entirely in `sniper_belt.py`'s `on_signal()`, same style as existing refusals.

Confirm and I'll write it: any objection to gating "missed" on process-boot-time rather than a fixed minute count?
---

Notes: no ORDER-xxx id referenced. This is a design proposal awaiting Michael's confirmation, not a completed work order. Relates to tonight's naked-position incident (a ~3h39m-stale signal replayed after the desk outage, submitted with an inverted stop).
